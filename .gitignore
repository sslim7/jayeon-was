.idea/

# go build 산출물 (모듈명과 같은 이름의 바이너리)
/nature-was

# 환경변수 (커밋 금지) — 템플릿은 .env.example 참고
.env
.env.*
!.env.example

# 에뮬레이터(Firestore) 로컬 데이터/로그
emulator-data*/
*-debug.log
firebase-debug.log

# `firebase emulators:export` 산출물 — 실행할 때마다 타임스탬프 디렉터리가 새로 생긴다
firebase-export-*/

# macOS
.DS_Store

# Claude worktrees/세션 파일
.claude/

# Playwright MCP 임시 아티팩트
.playwright-mcp/

# 서비스 계정 키 등 자격증명 (커밋 금지)
secrets/

# 이전 이름으로 생성한 로컬 바이너리도 제외한다.
/jayeon-was
